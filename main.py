import numpy as np 

def sample_mean(m) : 
  # Your code to calculate a  sample mean from m 
  # uniform random variables between 0 and 1 goes here
  
  
def variance(n,m) : 
  # Your code to estimate the variance for a set of n 
  # sample means, which are each computed from m 
  # uniform random variables between 0 and 1 goes here.
  
  

# This code shouldn't need to be modified.  It will output three 
# estimates of the variance of a sample mean computed from 
# 10 uniform random variables.
print( variance( 100, 10), variance( 100, 10), variance( 100, 10) )
